# Android Intelligence Suite assets
These files are injected by MainActivity into the live ECRINTEL WebView so Android-only suite features appear even before the web feature branch is merged to main.
